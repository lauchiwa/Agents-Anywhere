.gradle/
.kotlin/
build/
local.properties

**/build/
**/.cxx/
**/.externalNativeBuild/

*.iml
.idea/

captures/
*.apk
*.aab
*.ap_
*.hprof

# Signing keys — NEVER commit. Keystores and their passwords stay local
# (passwords live in local.properties, already ignored above). Losing this
# key means you can't ship updates to an already-installed release build.
*.jks
*.keystore
keystore/
release-keystore/
